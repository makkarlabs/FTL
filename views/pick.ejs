<% include head.ejs %>

<% include header.ejs %>
    <div class="container">
        <div class="row">
            <% for(i=0; i < 8; i++) { %>
            <div class="col-md-3">
                <div class="playerIcon"></div>
            </div>
            <% } %>
        </div>
        <div class="row">
            <% for(i=0; i < players.length; i++) { %>
            <div class="col-md-3">
                <div class="playerIcon">
                    <img src="<%= players[i].pic %>"/>
                    <p><%= players[i].name %></p>
                </div>
            </div>
            <% } %>
        </div>
        <div class="row">
        </div>
    </div>

<% include footer.ejs %>
